--- a/drivers/gpu/drm/msm/adreno/a5xx_catalog.c
+++ b/drivers/gpu/drm/msm/adreno/a5xx_catalog.c
@@ -11,6 +11,19 @@
 
 static const struct adreno_info a5xx_gpus[] = {
 	{
+		.chip_ids = ADRENO_CHIP_IDS(0x05000400),
+		.family = ADRENO_5XX,
+		.revn = 504,
+		.fw = {
+			[ADRENO_FW_PM4] = "a530_pm4.fw",
+			[ADRENO_FW_PFP] = "a530_pfp.fw",
+		},
+		.gmem = (SZ_128K + SZ_8K),
+		.inactive_period = DRM_MSM_INACTIVE_PERIOD,
+		.quirks = ADRENO_QUIRK_TWO_PASS_USE_WFI |
+			  ADRENO_QUIRK_LMLOADKILL_DISABLE,
+		.funcs = &a5xx_gpu_funcs,
+	}, {
 		.chip_ids = ADRENO_CHIP_IDS(0x05000500),
 		.family = ADRENO_5XX,
 		.revn = 505,
--- a/drivers/gpu/drm/msm/adreno/adreno_gpu.h
+++ b/drivers/gpu/drm/msm/adreno/adreno_gpu.h
@@ -376,6 +376,11 @@
 	return adreno_is_revn(gpu, 430);
 }
 
+static inline int adreno_is_a504(const struct adreno_gpu *gpu)
+{
+	return adreno_is_revn(gpu, 504);
+}
+
 static inline int adreno_is_a505(const struct adreno_gpu *gpu)
 {
 	return adreno_is_revn(gpu, 505);
--- a/drivers/gpu/drm/msm/adreno/a5xx_gpu.c
+++ b/drivers/gpu/drm/msm/adreno/a5xx_gpu.c
@@ -449,7 +449,8 @@
 	const struct adreno_five_hwcg_regs *regs;
 	unsigned int i, sz;
 
-	if (adreno_is_a505(adreno_gpu) || adreno_is_a506(adreno_gpu) ||
+	if (adreno_is_a504(adreno_gpu) || adreno_is_a505(adreno_gpu) ||
+	    adreno_is_a506(adreno_gpu) ||
 	    adreno_is_a508(adreno_gpu)) {
 		regs = a50x_hwcg;
 		sz = ARRAY_SIZE(a50x_hwcg);
@@ -494,7 +495,8 @@
 	OUT_RING(ring, 0x00000000);
 
 	/* Specify workarounds for various microcode issues */
-	if (adreno_is_a505(adreno_gpu) || adreno_is_a506(adreno_gpu) ||
+	if (adreno_is_a504(adreno_gpu) || adreno_is_a505(adreno_gpu) ||
+	    adreno_is_a506(adreno_gpu) ||
 	    adreno_is_a530(adreno_gpu)) {
 		/* Workaround for token end syncs
 		 * Force a WFI after every direct-render 3D mode draw and every
@@ -764,11 +766,12 @@
 		0x00100000 + adreno_gpu->info->gmem - 1);
 	gpu_write(gpu, REG_A5XX_UCHE_GMEM_RANGE_MAX_HI, 0x00000000);
 
-	if (adreno_is_a505(adreno_gpu) || adreno_is_a506(adreno_gpu) ||
+	if (adreno_is_a504(adreno_gpu) || adreno_is_a505(adreno_gpu) ||
+	    adreno_is_a506(adreno_gpu) ||
 	    adreno_is_a508(adreno_gpu) || adreno_is_a510(adreno_gpu)) {
 		gpu_write(gpu, REG_A5XX_CP_MEQ_THRESHOLDS, 0x20);
-		if (adreno_is_a505(adreno_gpu) || adreno_is_a506(adreno_gpu) ||
-			adreno_is_a508(adreno_gpu))
+		if (adreno_is_a504(adreno_gpu) || adreno_is_a505(adreno_gpu) ||
+		    adreno_is_a506(adreno_gpu) || adreno_is_a508(adreno_gpu))
 			gpu_write(gpu, REG_A5XX_CP_MERCIU_SIZE, 0x400);
 		else
 			gpu_write(gpu, REG_A5XX_CP_MERCIU_SIZE, 0x20);
@@ -784,7 +787,8 @@
 		gpu_write(gpu, REG_A5XX_CP_ROQ_THRESHOLDS_1, 0x40201B16);
 	}
 
-	if (adreno_is_a505(adreno_gpu) || adreno_is_a506(adreno_gpu) ||
+	if (adreno_is_a504(adreno_gpu) || adreno_is_a505(adreno_gpu) ||
+	    adreno_is_a506(adreno_gpu) ||
 	    adreno_is_a508(adreno_gpu))
 		gpu_write(gpu, REG_A5XX_PC_DBG_ECO_CNTL,
 			  (0x100 << 11 | 0x100 << 22));
@@ -803,7 +807,8 @@
 	 * Disable the RB sampler datapath DP2 clock gating optimization
 	 * for 1-SP GPUs, as it is enabled by default.
 	 */
-	if (adreno_is_a505(adreno_gpu) || adreno_is_a506(adreno_gpu) ||
+	if (adreno_is_a504(adreno_gpu) || adreno_is_a505(adreno_gpu) ||
+	    adreno_is_a506(adreno_gpu) ||
 	    adreno_is_a508(adreno_gpu) || adreno_is_a509(adreno_gpu) ||
 	    adreno_is_a512(adreno_gpu))
 		gpu_rmw(gpu, REG_A5XX_RB_DBG_ECO_CNTL, 0, (1 << 9));
@@ -1403,8 +1408,9 @@
 	u32 mask = 0xf;
 	int i, ret;
 
-	/* A505, A506, A508, A510 have 3 XIN ports in VBIF */
-	if (adreno_is_a505(adreno_gpu) || adreno_is_a506(adreno_gpu) ||
+	/* A504, A505, A506, A508, A510 have 3 XIN ports in VBIF */
+	if (adreno_is_a504(adreno_gpu) || adreno_is_a505(adreno_gpu) ||
+	    adreno_is_a506(adreno_gpu) ||
 	    adreno_is_a508(adreno_gpu) || adreno_is_a510(adreno_gpu))
 		mask = 0x7;
 
--- a/drivers/soc/qcom/ubwc_config.c
+++ b/drivers/soc/qcom/ubwc_config.c
@@ -272,6 +272,7 @@
 	{ .compatible = "qcom,sc8180x", .data = &sc8180x_data, },
 	{ .compatible = "qcom,sc8280xp", .data = &sc8280xp_data, },
 	{ .compatible = "qcom,sda660", .data = &msm8937_data },
+	{ .compatible = "qcom,sdm429", .data = &msm8937_data },
 	{ .compatible = "qcom,sdm450", .data = &msm8937_data },
 	{ .compatible = "qcom,sdm630", .data = &msm8937_data },
 	{ .compatible = "qcom,sdm632", .data = &msm8937_data },
