# Desktop sessions use the selected hardware output directly. The mobile
# role loopbacks can leave PulseAudio clients waiting for stream activation.
wireplumber.profiles = {
  main = {
    policy.linking.role-based.loopbacks = disabled
  }
}
