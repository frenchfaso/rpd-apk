#!/bin/sh
# Rebuild boot files using the distribution deviceinfo after this package's
# override is removed. mkinitfs also performs the normal boot-deploy step.
command -v mkinitfs >/dev/null 2>&1 || exit 0
mkinitfs || echo 'Re-run mkinitfs before reboot to restore the distribution DTB selection.' >&2
exit 0
