# Alpine's Chromium wrapper sources this file; retain its sandbox defaults.
if [ "${XDG_CURRENT_DESKTOP:-}" = labwc ]; then
    CHROMIUM_FLAGS="$CHROMIUM_FLAGS --ozone-platform=wayland --enable-wayland-ime"
fi
