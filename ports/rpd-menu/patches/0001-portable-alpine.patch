--- a/src/properties.c
+++ b/src/properties.c
@@ -114,8 +114,9 @@
     {
         MenuCacheItem *item = (MenuCacheItem *) data;
 
-        char *args[] = { "rpcc", "main_menu", "on_menu_edit", (char *) menu_cache_item_get_file_basename (item), NULL };
-        execvp ("rpcc", args);
+        char *args[] = { "rpd-edit-launcher", (char *) menu_cache_item_get_file_path (item), NULL };
+        execvp ("rpd-edit-launcher", args);
+        _exit (127);
     }
     gtk_widget_destroy (dlg);
 }
